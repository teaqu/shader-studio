float twice(float value) {
    return value * 2.0;
}

// Macros, appended so the declarations above keep their line numbers.
#define uKaleido 0.65
#define uKGlow vec3(0.55, 0.30, 1.00)
#define lfo(rate, phase) ((rate) + (phase))
